import { CheckEmailExistsDirective } from './check-email-exists.directive';

describe('CheckEmailExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckEmailExistsDirective();
    expect(directive).toBeTruthy();
  });
});
