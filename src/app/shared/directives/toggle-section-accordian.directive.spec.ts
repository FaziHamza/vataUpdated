import { ToggleSectionAccordianDirective } from './toggle-section-accordian.directive';

describe('ToggleSectionAccordianDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSectionAccordianDirective();
    expect(directive).toBeTruthy();
  });
});
